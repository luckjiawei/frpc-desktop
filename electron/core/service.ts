/**
 * BaseService is an abstract class that serves as a foundation for other service classes.
 * It can be extended to implement specific service functionalities.
 */
export default abstract class BaseService {}
